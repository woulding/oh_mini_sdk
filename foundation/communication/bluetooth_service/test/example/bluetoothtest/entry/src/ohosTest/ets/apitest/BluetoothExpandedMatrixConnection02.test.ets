/**
 * Copyright (C) 2022 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * ConnectivityKit `getProfileConnectionState` 扫参 **130～259**（展开写法）。与 `BluetoothExpandedMatrixConnection01.test` 合起来覆盖 0～259。
 */
import { describe, it, expect} from '@ohos/hypium';
import { connection, constant} from '@kit.ConnectivityKit';
import { matrixTryRun} from './BluetoothExpandedMatrixShared';

export default function bluetoothExpandedMatrixConnection02Test(){
  describe('ExpandedMatrix_CK_getProfileConnectionState_130_259', () => {
    it('expanded_ck_getProfileConnectionState_130', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(130 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_131', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(131 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_132', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(132 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_133', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(133 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_134', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(134 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_135', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(135 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_136', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(136 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_137', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(137 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_138', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(138 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_139', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(139 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_140', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(140 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_141', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(141 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_142', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(142 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_143', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(143 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_144', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(144 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_145', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(145 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_146', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(146 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_147', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(147 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_148', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(148 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_149', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(149 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_150', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(150 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_151', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(151 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_152', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(152 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_153', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(153 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_154', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(154 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_155', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(155 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_156', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(156 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_157', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(157 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_158', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(158 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_159', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(159 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_160', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(160 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_161', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(161 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_162', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(162 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_163', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(163 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_164', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(164 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_165', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(165 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_166', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(166 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_167', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(167 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_168', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(168 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_169', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(169 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_170', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(170 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_171', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(171 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_172', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(172 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_173', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(173 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_174', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(174 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_175', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(175 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_176', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(176 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_177', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(177 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_178', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(178 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_179', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(179 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_180', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(180 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_181', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(181 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_182', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(182 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_183', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(183 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_184', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(184 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_185', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(185 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_186', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(186 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_187', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(187 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_188', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(188 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_189', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(189 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_190', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(190 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_191', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(191 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_192', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(192 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_193', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(193 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_194', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(194 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_195', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(195 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_196', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(196 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_197', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(197 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_198', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(198 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_199', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(199 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_200', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(200 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_201', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(201 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_202', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(202 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_203', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(203 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_204', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(204 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_205', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(205 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_206', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(206 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_207', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(207 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_208', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(208 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_209', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(209 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_210', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(210 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_211', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(211 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_212', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(212 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_213', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(213 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_214', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(214 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_215', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(215 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_216', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(216 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_217', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(217 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_218', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(218 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_219', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(219 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_220', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(220 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_221', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(221 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_222', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(222 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_223', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(223 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_224', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(224 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_225', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(225 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_226', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(226 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_227', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(227 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_228', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(228 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_229', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(229 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_230', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(230 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_231', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(231 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_232', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(232 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_233', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(233 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_234', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(234 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_235', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(235 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_236', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(236 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_237', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(237 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_238', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(238 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_239', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(239 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_240', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(240 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_241', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(241 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_242', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(242 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_243', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(243 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_244', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(244 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_245', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(245 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_246', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(246 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_247', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(247 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_248', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(248 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_249', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(249 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_250', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(250 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_251', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(251 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_252', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(252 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_253', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(253 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_254', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(254 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_255', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(255 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_256', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(256 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_257', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(257 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_258', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(258 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

    it('expanded_ck_getProfileConnectionState_259', 0, () => {
      matrixTryRun(() => {
        connection.getProfileConnectionState(259 as constant.ProfileId);
      });
      expect(true).assertTrue();
    });

  });
}
