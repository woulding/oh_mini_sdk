Security
    * Add tag length validation in mebdtls_ccm_finish() to prevent
      out-of bounds reads and mitigate potential application buffer
      overflows whwere applications relied on the library to enforce
      tag length constraints.